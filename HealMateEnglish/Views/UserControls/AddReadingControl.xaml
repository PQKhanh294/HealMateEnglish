<UserControl x:Class="HealMateEnglish.Views.UserControls.AddReadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:HealMateEnglish.Converters"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">
    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D89EF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderBrush" Value="#2D89EF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1761A0"/>
                    <Setter Property="BorderBrush" Value="#1761A0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="#B0BEC5"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="Background" Value="#F4F8FB"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Border Background="White" CornerRadius="10" Padding="24" BorderBrush="#E3EAF2" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Add Reading Topic" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" Foreground="#1B263B"/>
            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,10">
                <TextBlock Text="Reading Topic Title:" FontWeight="SemiBold"/>
                <TextBox x:Name="TitleTextBox" Style="{StaticResource InputTextBoxStyle}" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Generate Content (AI Gemini)" Command="{Binding GeneratePassageCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="260" Margin="0,0,0,10"/>
                <TextBlock Text="Reading Passage:" FontWeight="SemiBold"/>
                <TextBox x:Name="PassageTextBox" Style="{StaticResource InputTextBoxStyle}" Height="100" AcceptsReturn="True" TextWrapping="Wrap" Text="{Binding PassageText, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Save Reading Topic" Style="{StaticResource PrimaryButtonStyle}" Width="140" Command="{Binding SaveCommand}"/>
                <Button Content="Clear" Style="{StaticResource PrimaryButtonStyle}" Background="#B0BEC5" BorderBrush="#B0BEC5" Foreground="#263238" Width="100" Command="{Binding ClearCommand}"/>
                <Button Content="Regenerate" Style="{StaticResource PrimaryButtonStyle}" Width="120" Command="{Binding GeneratePassageCommand}"/>
            </StackPanel>
            <TextBlock Grid.Row="4" Text="{Binding ResultMessage}" Foreground="Green" FontWeight="SemiBold" Margin="0,20,0,0" HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl> 