<UserControl x:Class="HealMateEnglish.Views.UserControls.AddTopicControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D89EF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderBrush" Value="#2D89EF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1761A0"/>
                    <Setter Property="BorderBrush" Value="#1761A0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="BorderBrush" Value="#B0BEC5"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="Background" Value="#F4F8FB"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="36"/>
        </Style>
    </UserControl.Resources>
    <Border Background="White" CornerRadius="10" Padding="24" BorderBrush="#E3EAF2" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Add Writing Topic" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" Foreground="#1B263B"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Band:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <ComboBox x:Name="BandComboBox" Width="80" Margin="10,0,0,0" ItemsSource="{Binding Bands}" SelectedItem="{Binding Band, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,10">
                <TextBlock Text="Writing Topic Title:" FontWeight="SemiBold"/>
                <TextBox x:Name="TitleTextBox" Style="{StaticResource InputTextBoxStyle}" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="Generate Title (AI Gemini)" Command="{Binding GenerateTitleCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="220"/>
                    <Button Content="Regenerate" Command="{Binding GenerateTitleCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="120"/>
                </StackPanel>
            </StackPanel>
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Save Writing Topic" Style="{StaticResource PrimaryButtonStyle}" Width="140" Command="{Binding SaveCommand}"/>
                <TextBlock Text="{Binding ResultMessage}" Foreground="Green" FontWeight="SemiBold" Margin="20,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl> 