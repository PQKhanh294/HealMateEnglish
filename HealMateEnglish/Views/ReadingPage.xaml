<Page x:Class="HealMateEnglish.Views.ReadingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HealMateEnglish.Views"
      xmlns:converters="clr-namespace:HealMateEnglish.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1000"
      Title="Reading Practice">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:BooleanToVisibilityInverseConverter x:Key="InvBoolToVis"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Reading Practice" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <RadioButton x:Name="rbPreset" Content="Choose from presets" 
                         Margin="0,0,20,0" IsChecked="True" Checked="RadioButton_Checked"/>
            <RadioButton x:Name="rbCustom" Content="Enter your own text" 
                         Checked="RadioButton_Checked"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid x:Name="PresetGrid" Visibility="{Binding IsPresetMode, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <CheckBox Grid.Row="0" Content="Show only AI-created presets" 
                          IsChecked="{Binding ShowAiCreatedOnly}" Margin="0,0,0,10"/>
                <TextBlock Grid.Row="1" Text="Select a reading passage:" Margin="0,0,0,10"/>
                <ListView Grid.Row="2" x:Name="PresetListView" 
                          ItemsSource="{Binding FilteredPresetReadings}" 
                          SelectedItem="{Binding SelectedPresetReading}"
                          BorderThickness="1" BorderBrush="#DDDDDD">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{Binding Title}" 
                                           FontWeight="Bold" FontSize="16"/>
                                <TextBlock Grid.Row="1" Text="{Binding Part}" 
                                           Foreground="Gray" FontStyle="Italic"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <Grid x:Name="CustomGrid" Visibility="{Binding IsPresetMode, Converter={StaticResource InvBoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Enter your text:" Margin="0,0,0,10"/>
                <TextBox Grid.Row="1" x:Name="CustomTextBox" 
                         AcceptsReturn="True" TextWrapping="Wrap"
                         Text="{Binding CustomPassage, UpdateSourceTrigger=PropertyChanged}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         BorderThickness="1" BorderBrush="#DDDDDD"/>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Width="100" Height="35" Margin="0,0,15,0"
                    Click="CancelButton_Click"/>
            <Button Content="Begin" Width="150" Height="35" 
                    Background="#007BFF" Foreground="White"
                    Command="{Binding GenerateQuestionsCommand}"/>
        </StackPanel>
    </Grid>
</Page>
